# math-verify.station

## Purpose
Station wrapper for `X:\Models\M11_math_verify`.

Parses equations and numerical claims, then checks arithmetic/algebraic consistency.

## Status
Skeleton. Model slot is empty except `desktop.ini`; this is expected if using the `math-verify` Python package.

## Front Door
`X:\Models\front-doors\11_math_verify_FD\config.json`
