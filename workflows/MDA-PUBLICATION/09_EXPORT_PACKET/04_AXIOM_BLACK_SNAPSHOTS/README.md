# Axiom Black Snapshots

Front-and-center review prompt:

`00_FRONT_AND_CENTER_APPROVAL_PROMPT.md`

Open `index.html`.

This is a black-panel inspection view for the 61 paper snapshots. It is best for:

- seeing each paper's truth/coherence/chi posture
- finding risky/supporting sentences
- reviewing claim surface counts
- opening raw JSON snapshots

The formal audit fields inside some raw snapshots are sparse. The visible view uses the richer pipeline metrics where the formal fields are empty.
