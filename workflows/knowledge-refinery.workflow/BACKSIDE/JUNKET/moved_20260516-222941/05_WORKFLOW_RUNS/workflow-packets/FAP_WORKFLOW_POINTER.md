# FAP Workflow Pointer

The FAP lane is the folder automation / pipeline spine:

```text
X:\knowledge-refinery\13_SOURCE_SYSTEMS\FAP
```

Control-plane packet:

```text
X:\github\pipeline-workflows\workflows\FolderAutomationPipeline
```

Core line:

```text
intake -> classified -> media-routed -> lossless -> vectorized -> graded -> axiom-mapped -> output
```

This is the lane to build into the tracked folder transport / Postgres ledger workflow.
