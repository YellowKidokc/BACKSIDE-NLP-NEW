# FAP X Runtime Copy

This is the X-drive promoted copy of:

```text
D:\FAP
```

Use this X path for Brain/refinery work:

```text
X:\knowledge-refinery\13_SOURCE_SYSTEMS\FAP
```

## What It Contains

- `wiki` - FAP system notes, prompts, and architecture docs.
- `wiki-compiler` - small local wiki/compiler repo with markdown sources and `.olw` state.
- `intake`, `classified`, `graded`, `axiom-mapped`, `lossless`, `vectorized`, `output`, `logs` - workflow lanes.

## Verification

The D and X file lists currently match by relative path and size.

Git note: `wiki-compiler` may require a Git safe-directory exception when accessed from X/NAS paths.

## Rule

Treat X as the runtime copy. Keep `D:\FAP` as fallback until we intentionally retire or redirect it.
