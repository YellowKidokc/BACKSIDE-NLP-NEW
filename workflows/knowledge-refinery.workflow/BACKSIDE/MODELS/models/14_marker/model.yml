model_id: marker_pdf_structural_converter
provider: tool_wrapper
role: pdf_structural_conversion
status: placeholder

runtime:
  engine: marker

capabilities:
  - pdf_structured_extract
  - equation_extract
  - table_extract

station_use:
  - 03_cleaner
  - 10_math_checker

healthcheck:
  prompt: "Return JSON: {\"status\":\"ok\"}"
  expected_contains: "\"status\""

outputs:
  required_format: markdown

