# Academic Projection

Projection station for formal academic rewrites.

This station consumes canonical Markdown, `paper-snapshot.json`, and
`trace-ledger.json`. It emits a formal academic version plus a claim-to-trace
map. It must preserve epistemic tiers and defeat conditions.

Failure mode to watch: making the paper sound more academically finished than
the snapshot justifies.
